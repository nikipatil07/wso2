environments:
  - name: dev
    endpoints:
      production:
		url: http://dev.wso2.com:9090/samplestore
		config:
            retryTimeOut: $RETRY
      sandbox:
		url: http://dev.wso2.com:9090/samplestore
		config:
            retryTimeOut: $RETRY
  - name: prod
    endpoints:
      production:
		url: http://dev.wso2.com:9090/samplestore
		config:
            retryTimeOut: $RETRY
      sandbox:
		url: http://dev.wso2.com:9090/samplestore
		config:
            retryTimeOut: $RETRY